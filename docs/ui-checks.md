# 介面驗證紀錄

日期：2026-09-16。瀏覽器檢查使用隔離的臨時資料庫，測試帳號只在 `scripts/ui-qa-server.py` 的 localhost 8766 服務存在，不會加入正式平台。

已實際操作：

- 登入、任務工作台與沒有 GPU 時的空白狀態。
- 切換圖片服務，經檔案選擇器上傳原創 calibration.png。
- 填寫批次名稱、標記示範資料、提交並看到排隊狀態。
- 工作詳情顯示等待原因、執行次數與未分配狀態。
- 取消確認對話框，確認工作變成已取消。
- 管理頁看到同一筆工作的狀態，統計未把取消列入完成率。
- 390×844 手機尺寸的選單、管理頁與展示總覽。表格可水平捲動。
- 檢查桌面工作台的視覺版面。

- 隔離測試站也執行真正的 CUDA 工作，並非填入假成果。
- 圖片完成後開啟原圖與放大圖，拖曳比較滑桿（键盤右鍵 50→51），PNG 下載事件完成。
- 音訊完成後看到實際逐字稿內容，SRT 下載事件完成。
- 設備設定顯示 RTX 3050、4 GB、驅動 580.97、作業系統與兩種服務測試結果。
- 結果預覽檢查當時的瀏覽器錯誤紀錄為空。
- 停止隔離的中央測試服務後，畫面顯示「連線中斷，重新連線中」，標示目前為最後收到的狀態並提供重試。

前端正式編譯通過。這些是人工操作瀏覽器的驗證紀錄，沒有宣稱提供自動端到端瀏覽器測試。

## HTTPS

使用 `scripts/create-certs.py` 產生的本機測試 CA 與 SAN 憑證，啟動隔離資料目錄的 HTTPS 8767 服務，HTTP 用戶端載入 CA 後成功讀取健康檢查。未使用 `verify=False`。這不代表其他兩台電腦已完成憑證信任設定。

